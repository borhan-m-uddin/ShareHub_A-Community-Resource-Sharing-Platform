# Copy this to .env for local reference only.
# IMPORTANT: Do not commit secrets. Vercel environments should be set in Dashboard → Project → Settings → Environment Variables.

# --- Database ---
# Use your managed MySQL provider (not localhost on Vercel)
DB_SERVER=your-managed-db-hostname
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password
DB_NAME=community_sharing
DB_PORT=3306

# --- SMTP (email) ---
# Based on current storage/settings.json defaults for Gmail
SMTP_HOST=smtp.gmail.com
SMTP_USER=burhanuddin49945@gmail.com
# Use your Gmail App Password here locally, but NEVER commit it.
# In Vercel, set SMTP_PASS in Project Settings.
SMTP_PASS=
SMTP_PORT=587
SMTP_SECURE=tls
SMTP_FROM=burhanuddin49945@gmail.com
SMTP_FROM_NAME=ShareHub
