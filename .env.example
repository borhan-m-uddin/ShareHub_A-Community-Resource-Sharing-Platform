# Copy this to .env for local reference (Vercel uses Project Settings → Environment Variables)

# Database
DB_SERVER=your-db-hostname
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password
DB_NAME=community_sharing
DB_PORT=3306

# SMTP (email)
SMTP_HOST=smtp.yourprovider.com
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_PORT=587
SMTP_SECURE=tls
SMTP_FROM=no-reply@your-domain.com
SMTP_FROM_NAME=ShareHub
